# Streamlit Cloud Compatible Requirements
# =====================================
# 
# Minimal dependencies optimized for Streamlit Cloud deployment
# All heavy dependencies are made optional with fallbacks

# Core Streamlit and Web Framework
streamlit>=1.28.0
requests>=2.31.0

# Data Processing and Analysis
pandas>=2.0.0
numpy>=1.24.0

# Visualization and Plotting
plotly>=5.15.0

# Graph Processing (optional - has fallback)
networkx>=3.1

# HTTP Client
httpx>=0.24.0

# Utilities
python-dotenv>=1.0.0
typing-extensions>=4.7.0

# Date and Time Processing
python-dateutil>=2.8.2

# Optional dependencies (will use fallbacks if not available)
# Document Processing
PyPDF2>=3.0.1
python-docx>=0.8.11

# Vector Database (optional)
chromadb>=0.4.15

# Note: Heavy dependencies like OCR libraries, sentence-transformers, 
# and other ML libraries are intentionally excluded to ensure 
# compatibility with Streamlit Cloud's resource constraints.
# The application includes fallback mechanisms for all optional dependencies.
